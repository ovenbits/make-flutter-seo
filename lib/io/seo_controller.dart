// 🐦 Flutter imports:
import 'package:flutter/material.dart';

class SeoController extends StatelessWidget {
  final Widget child;

  const SeoController({super.key, bool enabled = false, required this.child});

  @override
  Widget build(BuildContext context) => child;
}
